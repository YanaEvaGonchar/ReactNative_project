import React from 'react';

import {Block} from '../styled/Block';
import {Text} from '../styled/Text';

const GPDWithRealm = () => {
  return (
    <Block>
      <Text>GPS with Realm</Text>
    </Block>
  );
};

export default GPDWithRealm;
